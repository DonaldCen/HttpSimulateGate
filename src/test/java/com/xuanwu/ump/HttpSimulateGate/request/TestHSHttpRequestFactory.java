package com.xuanwu.ump.HttpSimulateGate.request;

import com.xuanwu.ump.HttpSimulateGate.http.HSHttpTaskExecutor;

import junit.framework.TestCase;

/**
 * @Description
 * @Author <a href="mailto:cenyingqiang@wxchina.com">yingqiang.Cen</a>
 * @Date 2020/2/19
 * @Version 1.0.0
 */
public class TestHSHttpRequestFactory extends TestCase {
    public void testRequestPathXmlConfigRequest()throws Exception{
        /*HSHttpRequest request = HSHttpRequestFactory.getHttpRequest("downloadImage");
        // 若不传入word参数，默认使用配置的值
        //request.getContext().addInputData("word","宠物萌图");
        request.execute();
        // 等待所有线程执行完成
        HSHttpTaskExecutor.getInstance().waitForFinish(Thread.currentThread());*/
    }
}
